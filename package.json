{
  "name": "visualdynamics",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "storybook": "pnpm client storybook",
    "client": "pnpm --filter client",
    "server": "pnpm --filter server",
    "dev": "concurrently --kill-others \"pnpm:client dev\" \"pnpm:server dev\""
  },
  "prisma": {
    "schema": "apps/client/prisma/schema.prisma",
    "seed": "tsx apps/client/prisma/seed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
