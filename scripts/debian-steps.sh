#!bin/bash
# We'll assume Debian or variant
# Make sure what we need is installed
echo "==> Installing cmake, gcc, git, grace and unzip"
sudo apt install cmake gcc git grace unzip -y
echo "==> Installed cmake, gcc, git, grace and unzip"